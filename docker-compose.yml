# docker-compose.yml

version: '3.8'

services:
  app:
    container_name: nest-app
    build: .
    restart: always
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:wiBMYkdknLqNLKylqhkJLGmWvvHJHoJv@postgres.railway.internal:5432/railway
